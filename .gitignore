node_modules/
.env
*.log
__pycache__/
*.pyc
# Training trajectory logs are run artifacts, not source.
data/trajectories/*.jsonl

# Python / emulator backend
.venv/

# Copyrighted ROM and derived save data — never commit
roms/
*.gb
*.gb.ram

# PyBoy savestate (regenerate via `python -m emulator.make_state`)
emulator/overworld.state
