[
  {"_": "Continues where routes/opening.json ends: in Oak's Lab, starter in the party, rival battle won. Run both together — `--script` is repeatable, so this file never restates the opening. Its job is action balance: the opening is cutscene-heavy and harvests ~7 A-presses per move, while this leg is almost all walking plus whatever wild encounters Route 1 rolls."},

  {"_": "Lab -> outside. The rival's post-battle speech is still on screen when the opening ends — its three trailing A-presses are a hand-count and they under-fire; measured, Red is still frozen behind ': Okay! Il make my...' and EVERY move returns BLOCKED. So do not lead with a goto here. Cycle [a, south] instead, the same idiom the opening uses to leave this room the first time: while the text runs the south is a no-op and the A advances it; once Red is free the A is harmless and the south walks him down column 5 to the door, which is a bottom-edge warp, so continuing south is also what steps out of it. Gate on the map id, never on {dialogue: false} — that is true for a frame between two boxes."},
  {"press": [{"type": "a"}, {"type": "move", "direction": "south"}],
   "until": {"area": 0}, "max": 40},
  {"type": "wait"},

  {"_": "Pallet Town -> the Route 1 mouth at (10,1). The opening's goto to this same cell stops short because Oak intercepts; he is done now, so this one is expected to arrive."},
  {"goto": {"x": 10, "y": 1}},

  {"_": "North off the top edge of Pallet and into Route 1. A map boundary, not a warp — nothing appears in exits[] to aim at, so walk north until the map id says 12."},
  {"press": {"type": "move", "direction": "north"}, "until": {"area": 12}, "max": 8},

  {"_": "The length of Route 1, entering around (10,35) and climbing to its north edge. Staged waypoints rather than one far goto: the route is fenced, hedged and LEDGED, and the viewport shows ten blocks of it at a time. Aimed at distant (10,0) the planner wanders hunting a gap it cannot see — the 7/26 run drifted east along the y=14 ledge line into the dead-end pocket at (15,14), stalled, and the blind steps after it hopped one-way ledges into a corner (ledges read walkable going north but only permit south). Every cell below was walked by the successful 7/26 18:32 run; each hop stays near viewport range, and the (14,12) waypoint IS the y=14 ledge gap so the planner cannot miss it. Still grass the whole way — do_goto fights the encounters it triggers rather than stalling on them."},
  {"goto": {"x": 8, "y": 28}},
  {"goto": {"x": 12, "y": 26}},
  {"goto": {"x": 12, "y": 20}},
  {"goto": {"x": 9, "y": 16}},
  {"goto": {"x": 14, "y": 12}},
  {"goto": {"x": 10, "y": 8}},
  {"goto": {"x": 14, "y": 2}},
  {"goto": {"x": 10, "y": 0}},

  {"_": "Route 1 -> Viridian City, another map-boundary step."},
  {"press": {"type": "move", "direction": "north"}, "until": {"area": 1}, "max": 8},

  {"_": "Viridian City -> the Pokémon Centre door at (23,25), then one more north through the warp."},
  {"exit_map": 41},
  {"type": "move", "direction": "north"},
  {"type": "wait"},

  {"_": "Up to the nurse's counter. Walking blind north rather than a goto: safe because the counter stops Red at y=3, and `pos` is still a world fact rather than a press count. This used to be the ONLY option — the interior decoded every floor tile as '#' because classify_block demanded all four tiles of a movement block be walkable, and navigate.py treats '#' as a definite obstacle. Fixed 2026-07-26 (the engine only consults the block's bottom-left tile), so a goto plans in here now; this leg stays as-is because it works and re-verifying it costs a full route replay."},
  {"press": {"type": "move", "direction": "north"},
   "until": {"pos": {"x": 3, "y": 3}}, "max": 8},

  {"_": "Heal. The nurse's sequence is a greeting, a HEAL/CANCEL prompt that defaults to HEAL, and the machine animation — 13 A-presses when measured, and nothing guarantees that number. party_healthy watches the HP itself, which is the only fact the world exposes at the end of it. Full HP is also what makes this leg composable: the next one starts from a known state instead of whatever Route 1 left behind."},
  {"type": "talk"},
  {"press": {"type": "a"}, "until": {"party_healthy": true}, "max": 30},

  {"_": "Back out into Viridian so the run does not end mid-textbox — the same handoff that broke the start of this file."},
  {"press": [{"type": "a"}, {"type": "move", "direction": "south"}],
   "until": {"area": 1}, "max": 30}
]
